export type DamConfig = {
  damHeight: number;
  damLeftWidth: number;
  damRightWidth: number;
  damTopWidth: number;
  //seaLevel: number;
};

export type SensorConfig = {
  name: string;
  value: number;
  distance: number;
};
